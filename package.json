{
  "name": "erlass-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild app.js --bundle --minify --outfile=app.bundle.js",
    "build:dev": "esbuild app.js --bundle --outfile=app.bundle.js --watch"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}


